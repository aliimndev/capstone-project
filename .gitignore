# Node
node_modules/
.next/
dist/
build/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
.pytest_cache/

# Environment & secrets
.env
.env.local
.env.*.local

# OS & tooling
.DS_Store
.vercel
.wrangler

# ML model artifact — 215 MB, hosted on Hugging Face, downloaded at container runtime.
# Never commit the .pkl to git; the .gitkeep preserves the empty directory for local dev.
apps/api/model/
!apps/api/model/.gitkeep
